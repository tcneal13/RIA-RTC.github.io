'use strict';
// defining a root controller
nasaApp.controller('RootController', ['$scope',
    // defining function in scope 
    function ($scope) {
        $scope.appTitle = 'nasaApp';
    }
]);
